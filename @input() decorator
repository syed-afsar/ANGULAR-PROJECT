@input() decorator: Passing data from parent to child component
---------------------------------------------------------------
Parent:

typescript:
name:string = 'syed';
---------------------------------------------------------------
HTML:
<app-gen [recname]="name"></app-gen>
---------------------------------------------------------------
---------------------------------------------------------------

Child:

typescript:
import { Component, OnInit, Input } from '@angular/core';
@Input() recname:string ;

---------------------------------------------------------------
HTML:
<p>name: {{recname}}</p>

---------------------------------------------------------------
---------------------------------------------------------------
